---
description: Build, deploy, and verify the site
---
Deploy the QuantHQ site:

1. Run `npm run build` from site/ directory
2. Check for build errors or warnings
3. If build succeeds, run `git add -A && git status`
4. Show me what will be committed — do NOT commit yet
5. Wait for my confirmation
6. After I confirm, commit with a descriptive message
7. Push to origin main
8. Verify deployment by fetching https://quanthq.in and checking it loads
9. Report: commit hash, deploy status, any issues
